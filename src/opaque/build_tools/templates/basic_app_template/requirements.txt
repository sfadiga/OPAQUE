# Requirements for basic OPAQUE framework application
# Install with: pip install -r requirements.txt

# OPAQUE Framework (install from source or PyPI when available)
opaque-framework

# Optional build dependencies (uncomment as needed)
# pyinstaller>=5.0  # For PyInstaller builds
# nuitka>=1.8       # For Nuitka builds

# Optional dependencies for enhanced functionality
# pillow>=9.0       # For image processing
# requests>=2.28    # For HTTP requests
